{
  "name": "youtube-downloader",
  "version": "1.0.0",
  "description": "Descargador de videos de YouTube usando Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "clean": "rm -rf node_modules package-lock.json",
    "prepare": "npm install"
  },
  "dependencies": {
    "ytdl-core": "^4.11.5"
  },
  "devDependencies": {
    "wrangler": "3.90.0",
    "@cloudflare/workers-types": "4.20231121.0"
  },
  "engines": {
    "node": ">=16.13.0"
  }
}
